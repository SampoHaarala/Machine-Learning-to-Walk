input -> hidden layer -> output

21 feature = yksi joint x DoF (3 tai 1). Output 21 = force input jokaseen niveleen.

AI-usage: Except for small adjustements and fixes, most of the code and documentation is generated by AI. It was used to generate the Python base folder with SNN and MLP models. More personal focus was spent on the loss-function and model structure.
It was also used to generate the base for the data transfer web socket.
